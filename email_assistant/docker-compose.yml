version: "3.8"

services:
  email_assistant:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./src/memory:/app/src/memory
    environment:
      - PYTHONPATH=/app
